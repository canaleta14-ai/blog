# Variables de entorno para producción
# IMPORTANTE: NO subir este archivo a Git (ya está en .gitignore)

# Generar claves secretas con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=tu-clave-secreta-aqui-cambiar
JWT_SECRET_KEY=otra-clave-secreta-aqui-cambiar

# Base de datos (Railway/Render proveen esto automáticamente)
DATABASE_URL=postgresql://usuario:password@host:5432/database

# Entorno
FLASK_ENV=production

# CORS (dominios permitidos, separados por coma)
CORS_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com
